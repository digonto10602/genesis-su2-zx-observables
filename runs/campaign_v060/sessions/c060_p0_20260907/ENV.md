# Preflight environment

Interpreter: repository-local .mamba/envs/su2zx/bin/python; Python 3.11.16, NumPy 2.4.6, SciPy 1.17.1, Qiskit 2.5.2, Aer 0.17.2 (live import probe).

Claude /usage live PTY: current session 5% used (reset 7:50pm America/Denver), all-model week 23% used, Fable week 45% used (weekly reset Sep 12, 3am America/Denver). Displayed percentages are separate provider meters, not an allocation-share calculation. Reserve remaining permitted Fable capacity for mandatory planning and final sign-off; no optional Fable calls.

Codex quota: UNKNOWN pending retry. During /status interaction, a newline intended to skip an update instead accepted the update prompt. CLI output reports 0.153.2 -> 0.153.4 and installation outside the repository. Unintended scope violation, disclosed to user; no rollback attempted because that would make another out-of-scope installation change. Live codex --version confirms 0.153.4. No configuration or credential files were read by the orchestrator.

Full Nine-Month Plan not found under repository. Actual v0.5.0 prompt and its quoted objective, prior run PLAN.md, conventions.py, review and overlap analysis supplied to mandatory Fable planner. Requested historical filenames differ from actual layout; inventory.json records missing exact paths rather than inventing them.

Supervised session only. No unattended runner/durability claim. Planning subprocess capped at 600s with TERM and 20s kill grace. No QPU submission or Git push.
