# Session summary — c060_p0_20260909_4 (prompt v0.6.3)

Documentation and packaging only. No physics was changed, no gate was run, no
threshold, band, seed derivation, channel definition, Hamiltonian or Gauss-law
convention was touched.

## Previously undocumented `su2qc` modules and callables

All **23** modules and all **95** public top-level callables of
`runs/section8_v0.5.0_20260907T0628Z/src/su2qc/` were undocumented by any
generated inventory before this session; the string `su2qc` appeared in `docs/`
only inside a referenced review file. Public callables per module:

- `__init__.py` — 0 · `circuits/__init__.py` — 0 · `circuits/export_l12.py` — 0
- `circuits/strang_l12.py` — 15 · `circuits/synth_l12.py` — 4
- `compile/__init__.py` — 0 · `compile/route.py` — 6 · `compile/run_g4.py` — 2
- `conventions.py` — 7
- `dynamics/__init__.py` — 0 · `dynamics/engine.py` — 2 · `dynamics/run_g2.py` — 0 · `dynamics/scan.py` — 8
- `encodings/__init__.py` — 0 · `encodings/l12.py` — 5
- `ham/__init__.py` — 0 · `ham/compare.py` — 2 · `ham/limits.py` — 3 · `ham/route_gausskernel.py` — 11 · `ham/route_spinnet.py` — 16
- `replicate.py` — 4 · `twin/__init__.py` — 0 · `twin/twin.py` — 10

`docs/SU2QC_FUNCTION_INVENTORY.md` now carries 34 module sections and 203 entries;
`docs/SU2QC_CONTRACTS.md` carries 95 callable entries.

## The five mandatory R15 contract entries

| Entry | Recorded status |
|---|---|
| `run_counts(isa_meas_list, shots, seed, sim)` | invariant *repeats are independent* **VIOLATED** on the measured backend/path; caller-state mutation and in-loop re-transpilation recorded separately; repair **PROPOSED, NOT IMPLEMENTED — blocked on Fable** |
| `channel_weights` / `channel_closure_residual` closure identity | holds on the present basis, **basis-dependent, not structural**; exhaustive 82-code oracle found no violating code, so **no channel-definition change is warranted**; extending to j_max = 1 reintroduces the hazard silently |
| `channel_closure_residual` invalid-key behaviour | R9's demand for a nonzero residual is **UNSATISFIABLE AS WRITTEN** (both sides skip undecodable keys; measured residual 0.0), awaiting a ruling |
| `physical_yield` vs `postselect` on empty input | inconsistent contract recorded (`ZeroDivisionError` versus `({}, 0.0)`), **UNENFORCED** |
| `observables`, `matched_subtraction`, `bootstrap`, `postselect`, `twin_backend`, `add_measurements` | full entries; `bootstrap`'s two-sigma is an uncertainty **only** under independence, which the `run_counts` entry records as violated |

No status was upgraded anywhere. C0 stays **PARTIAL**, C1 **unamended**, C2 **NOT STARTED**.

## Graphify refresh (R17)

| | nodes | edges | communities |
|---|---:|---:|---:|
| before | 2075 | 2788 | 206 |
| after | 2482 | 3182 | 243 |

`su2qc` is inside the scan corpus (283 `su2qc` nodes). The 11 retained
out-of-corpus nodes came from `docs/REFERENCES.md`; that path was added to
`.graphifyignore` and the 11 nodes were pruned on rebuild, leaving 0. Community
labels were **not** regenerated by an LLM and are stale; `GRAPH_REPORT.md` says so
in its first screen, above the summary.

## Bundle (R18)

Shipped archive: `zip_results/SU2ZX_QUERY_2542af2_20260909T194325Z.zip`.

| | value |
|---|---|
| compressed | 1.84 MB (target 25 MB, cap 30 MB) |
| extracted | 5,460,766 bytes over 160 files (157 payload + 3 control) |
| largest included file | `graph/graph.json`, 2,680,358 bytes |
| total excluded | 169,297,882 bytes |

Excluded by reason class: circuit `.qpy`/`.qasm` exports 152,928,955 bytes in 8
files (hash plus qubit/CZ/2q-depth metadata recorded instead, shots not stored in
the artifact); Graphify AST cache and dated snapshots 4,153,530 bytes in 269
files; two v0.4.0 CSVs above 2 MB, 12,012,110 bytes, with a 500-row stratified
sample retained and labelled a sample; 12 PDF duplicates of included PNG figures,
203,287 bytes. The v0.6.2 credential screen ran verbatim over every included text
file with 0 hits; screening passed is not a guarantee of absence of every secret
format.

## Self-test (R19), on the extracted archive

| Check | Result |
|---|---|
| 1 · manifest completeness and SHA-256 | **pass** — 157 payload files listed and hashed, 160 present, 3 declared controls |
| 2 · every referenced path resolves | **pass** — 15 documents, 111 references, 0 dangling |
| 3 · source fidelity | **pass** — 68 source files byte-identical to the working tree, 200 legacy files still matching `diagnostic-source-manifest.json` |
| 4 · sizes | 5,460,766 bytes, 160 files, largest `graph/graph.json` at 2,680,358 bytes |
| 5 · `python -m compileall` over `src/` | **pass**, exit 0, syntax only |

`overall_pass: true`. Six builds were made; each is kept under its own name and
the two failing ones were not overwritten. `BUNDLE_REBUILDS.md` records what was
wrong with each.

## Reproduction

```
git log --oneline -6
python3 runs/campaign_v060/sessions/c060_p0_20260909_4/tools/build_bundle.py && python3 runs/campaign_v060/sessions/c060_p0_20260909_4/tools/selftest_bundle.py
```

## Blocking statement (verbatim, unchanged)

**Fable planning/escalation and final sign-off remain DEFERRED under budget policy;
C0 is PARTIAL, C1 is unamended, C2 is NOT STARTED; no production physics fix was
implemented; no hardware job has ever been submitted; every v0.5.0 twin σ is void.**

## Acceptance rows (section 3)

| Row | Result |
|---|---|
| Generator scope | pass — v0.4.0 inventory regenerates byte-identical to HEAD (`inventory-legacy.diff` empty); `docs/SU2QC_FUNCTION_INVENTORY.md` covers all 23 modules on the same schema |
| Contracts | pass — 95 entries; the five mandatory R15 entries carry the statuses named there |
| Logic doc | pass — `docs/SU2QC_LOGIC_AND_TESTS.md` carries the Section-8 Mermaid flow and per-module narrative |
| Status integrity | pass — no status upgraded; C0 PARTIAL / C1 unamended / C2 NOT STARTED stated in `INDEX.md`; noisy data labelled EMULATED |
| Graph | pass — `su2qc` in corpus, 11 out-of-corpus nodes retired, staleness on the report's first screen |
| Bundle | pass — 1.84 MB compressed, layout as R18, exclusions hashed and explained |
| Self-test | pass — all five R19 checks on the extracted archive |
| Cleanliness | **pass** — `git status` is empty. The prompts, both session directories and the tracked bundle tools are committed; the generated `artifacts/*.zip` joins `zip_results/` as ignored build output; 964 KB of Python caches were removed. Nothing was discarded: every evidence file, including the failed-attempt logs and partial drafts, is in history. See `git-status-final.txt` |

Session tool tests, all green: `session-tests-final.log` (8 inventory, 3 bundle,
1 graph-policy).
