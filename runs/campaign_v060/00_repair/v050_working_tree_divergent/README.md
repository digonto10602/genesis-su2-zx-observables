# Preserved v0.5.0 working-tree divergence

These four working-tree artifacts were preserved before restoring the committed HEAD versions as required by R2. The Trotter error arrays differ only at floating-point scale (maximum primary delta about 5.4e-15), while the derived log-slope fit is condition-sensitive. The signed evidence of record remains the committed HEAD version. See slope-conditioning.json.

